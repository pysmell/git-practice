package javabase.动态代理;

/**
 * 在java的动态代理机制中，有两个重要的类或接口，一个是InvocationHandler(Interface),另一个则是Proxy（Class）
 * MyInvocationHandler（interface）的描述：
 * 每一个动态代理类都必须要实现InvocationHandler这个接口，并且每个代理类的实例都关联到了一个handler，当我们
 */
public interface Interface {
}






























